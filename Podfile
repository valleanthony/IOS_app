# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'mop-connect' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for mop-connect

  target 'mop-connectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'mop-connectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
